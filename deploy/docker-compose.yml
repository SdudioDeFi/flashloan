version: '3.8'
services:
  backend:
    build: ../backend
    ports:
      - "4000:4000"
  frontend:
    build: ../frontend
    ports:
      - "3000:3000"
  monitoring:
    build: ../monitoring
    ports:
      - "5000:5000"
  bot:
    build: ../bot
